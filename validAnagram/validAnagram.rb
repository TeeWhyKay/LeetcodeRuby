# given two input strings determine if two strings are anagrams of each other
def valid_anagram?(first, second)
  # anagrams will have the same length and frequency of chars
  false if first.length != second.length

  #
end
